
<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-3">Fluid jumbotron</h1>
        <p class="lead">This is a modified jumbotron that occupies the entire horizontal space of its parent.</p>
    </div>
</div>

<h3>Reck</h3>
<h2>That</h2>
<h1>BODY!</h1>

<form>
    <div class="form-group">
    <label for="bodyInput"></label>
    <textarea class="form-control" id="bodyInput" rows="3"></textarea>
  </div>


<div class="form-row align-items-center">
    <div class="col-auto">
      <label class="sr-only" for="userId">UserId</label>
      <input type="text" class="form-control mb-2" id="userId" placeholder="user Id">
    </div>

    <div class="form-row align-items-center">
    <div class="col-auto">
      <label class="sr-only" for="threadId">thread Id</label>
      <input type="text" class="form-control mb-2" id="threadId" placeholder="thread Id">
    </div>

  <button type="button" class="btn btn-success submit">Submit Reply</button>
</form>

{{#each threads}}
{{#each Replies}}
<div class = "row">
    <div class ="col-md-8">
        <div class="card">
  <div class="card-body">
{{body}}
<button type="button" class="btn btn-danger" id="{{id}}">Delete</button>
</div>
</div>
</div>
</div>
{{/each}}
{{/each}}



{{#each threads}}
<p>{{id}}</p>
<p>{{body}}</p>
<p>{{CategoryId}}</p>

{{/each}}


